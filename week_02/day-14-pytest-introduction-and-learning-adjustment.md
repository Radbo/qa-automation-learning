# Day 14 — Pytest introduction and learning adjustment

Date: April 24, 2026

Today I started learning pytest and made an important adjustment to my learning plan.

## Pytest setup

- created a virtual environment (venv) inside the project
- installed pytest
- created a separate practice folder for pytest (`pytest-practice`)
- set up basic project structure with `tests/` directory

## First tests

- created utility functions:
  - `add()`
  - `divide()`
- wrote first pytest test cases:
  - positive scenarios
  - negative scenarios
  - edge cases (including division by zero)
- learned:
  - test file and function naming conventions (`test_`)
  - usage of `assert` in pytest
  - basic test execution with `python -m pytest`

## Git

- correctly added only relevant files to staging
- avoided committing unnecessary files (`venv`, `__pycache__`, `.pytest_cache`)
- created structured commit with meaningful message

## Key realization

- current Python level is not sufficient to move deeply into pytest and automation
- pytest is a tool that requires solid Python foundation
- initial plan was too aggressive in switching from Python to automation

## Plan adjustment

- shift focus back to Python fundamentals
- keep pytest practice at a basic level
- rebalance learning:
  - Python → main priority
  - pytest → secondary practice

## Result

- built first working pytest setup
- created initial test suite
- improved understanding of project structure and testing workflow
- corrected learning direction before accumulating technical gaps