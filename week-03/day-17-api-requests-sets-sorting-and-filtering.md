# Day 17 — Python: API requests, sets, sorting and filtering

Date: April 29, 2026

## API basics

- learned how to work with APIs using requests
- used `requests.get(url, params={})` to send GET requests
- understood structure of response object:
  - `response.text`
  - `response.content`
  - `response.json()`
- practiced extracting JSON data from responses

## Sets

- learned `set` data structure
- understood that sets store only unique values
- created sets using `set()`
- used:
  - `.add()` for adding elements
  - `.remove()` and `.discard()` for removing elements
- performed set operations:
  - `union()`
  - `intersection()`
  - `difference()`
- understood that sets are unordered

## Sorting and filtering

- used `sorted()` function:
  - default ascending order
  - `reverse=True` for descending
  - `key=` for custom sorting logic
- learned `filter()` function
- converted filter results into list

## Key learnings

- understood difference between:
  - response object and its data
  - JSON format and Python dictionary
- improved understanding of data processing:
  - filtering vs sorting
- reinforced importance of writing code alongside theory

## Problems / gaps

- confusion between `{}` (dict) and `set()`
- initial misunderstanding of response data types
- lack of sufficient hands-on practice

